# Python 3.11+ runtime for the full Baselane → Lofty pipeline.
# Install from the repository root with:
#   python3 -m pip install -r requirements.txt
#
# The editable Baselane MCP install keeps its package metadata in
# skills/baselane-mcp/pyproject.toml. Its uv.lock remains the preferred lock
# when installing that component with uv.
-e ./skills/baselane-mcp

# Canonical GL → Cash Flow workbook propagation and workbook reconciliation.
openpyxl>=3.1,<4

# Baselane/Lofty evidence and reviewed API clients.
requests>=2.31,<3
botocore>=1.34,<2

# Mortgage statement parsing and visible-browser CDP helpers.
pypdf>=4,<7
websocket-client>=1.8,<2

# Repository and deterministic accounting-policy regression checks.
pytest>=8,<9
